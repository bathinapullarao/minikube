apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perchases-chart.fullname" . }}-test-connection"
  labels:
    {{- include "perchases-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perchases-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
